{
  "name": "booking-app",
  "version": "0.1.0",
  "description": "Appointment and booking app",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx --yes serve . -l 3000",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
